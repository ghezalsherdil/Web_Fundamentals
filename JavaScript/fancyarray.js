var names = ["James", "Jill", "Jane", "Jack"];

function symbol(s) {
  for (var i =0; i < names.length; i++){
      // console.log(i + " -> " + names[i]);
      console.log(i + " " + s + " " + names[i]);
  }
}
symbol("=>")
symbol("->")
symbol("::")
symbol("----")
